






















# Auto setup for quick export




# updateSelectInput(session, "y_column", choices = names(data1$data), selected = "WordrecQuietZScore")
# updateTextInput(session = session, inputId = "y_title", value = "Word Recognition RTs (z-scores)")
# y_min <- -3
# y_max <- 3


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "Zind_AO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "Zind_AT")
# updateTextInput(session = session, inputId = "x_title", value = "RTs (z-scores, across participants)")
# x_min <- -1
# x_max <- 1

# wordZ vs. z-score

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "CR_AO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "CR_AT")
# updateTextInput(session = session, inputId = "x_title", value = "Confidence Ratings")
# x_min <- 0
# x_max <- 100

# wordZ vs. confidence

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "aprimeAO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "aprimeAT")
# updateTextInput(session = session, inputId = "x_title", value = "Sensitivity (A’)")
# x_min <- 0
# x_max <- 1

# wordZ vs. sens











# updateSelectInput(session, "y_column", choices = names(data1$data), selected = "Sentence_Rec_Quiet_Score")
# updateTextInput(session = session, inputId = "y_title", value = "Sentence Recognition Accuracy (%)")
# y_min <- 0
# y_max <- 100




# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "Zind_AO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "Zind_AT")
# updateTextInput(session = session, inputId = "x_title", value = "RTs (z-scores, across participants)")
# x_min <- -1
# x_max <- 1

# Sentence vs. z-score


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "CR_AO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "CR_AT")
# updateTextInput(session = session, inputId = "x_title", value = "Confidence Ratings")
# x_min <- 0
# x_max <- 100

# Sentence vs. confidence

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "aprimeAO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "aprimeAT")
# updateTextInput(session = session, inputId = "x_title", value = "Sensitivity (A’)")
# x_min <- 0
# x_max <- 1

# Sentence vs. sens












# updateSelectInput(session, "y_column", choices = names(data1$data), selected = "WordrecQuietScore")
# updateTextInput(session = session, inputId = "y_title", value = "Word Recognition Accuracy (%)")
# y_min <- 0
# y_max <- 100



# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "Zind_AO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "Zind_AT")
# updateTextInput(session = session, inputId = "x_title", value = "RTs (z-scores, across participants)")
# x_min <- -1
# x_max <- 1

# Word vs. z-scores

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "CR_AO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "CR_AT")
# updateTextInput(session = session, inputId = "x_title", value = "Confidence Ratings")
# x_min <- 0
# x_max <- 100

# Word vs. confidence


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "aprimeAO")
# updateSelectInput(session, "x_column2", choices = names(data1$data), selected = "aprimeAT")
# updateTextInput(session = session, inputId = "x_title", value = "Sensitivity (A’)")
# x_min <- 0
# x_max <- 1

# Word vs. sens





























updateSelectInput(session = session,inputId = "plotType",selected = "Scatterplot")













# updateSelectInput(session, "y_column", choices = names(data1$data), selected = "WordrecQuietZScore")
# updateTextInput(session = session, inputId = "y_title", value = "Word Recognition RTs (z-scores)")
# y_min <- -3
# y_max <- 3


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "Zind_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AO – AT RTs (z-scores, across participants)")
# x_min <- -1
# x_max <- 1

# Wordz vs. z-scores diff

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "CR_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AT – AO Confidence Ratings")
# x_min <- 0
# x_max <- 100

# Wordz vs. confidence diff


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "aprime_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AT – AO Sensitivity (A’)")
# x_min <- 0
# x_max <- 1

# Wordz vs. sens diff











# updateSelectInput(session, "y_column", choices = names(data1$data), selected = "Sentence_Rec_Quiet_Score")
# updateTextInput(session = session, inputId = "y_title", value = "Sentence Recognition Accuracy (%)")
# y_min <- 0
# y_max <- 100




# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "Zind_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AO – AT RTs (z-scores, across participants)")
# x_min <- -1
# x_max <- 1

# Sentence vs. z-scores diff

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "CR_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AT – AO Confidence Ratings")
# x_min <- 0
# x_max <- 100

# Sentence vs. confidence diff


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "aprime_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AT – AO Sensitivity (A’)")
# x_min <- 0
# x_max <- 1

# Sentence vs. sens diff












# updateSelectInput(session, "y_column", choices = names(data1$data), selected = "WordrecQuietScore")
# updateTextInput(session = session, inputId = "y_title", value = "Word Recognition Accuracy (%)")
# y_min <- 0
# y_max <- 100



# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "Zind_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AO – AT RTs (z-scores, across participants)")
# x_min <- -1
# x_max <- 1

# Word vs. z-scores diff

# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "CR_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AT – AO Confidence Ratings")
# x_min <- 0
# x_max <- 100

# Word vs. confidence diff


# updateSelectInput(session, "x_column", choices = names(data1$data),selected = "aprime_diff")
# updateTextInput(session = session, inputId = "x_title", value = "AT – AO Sensitivity (A’)")
# x_min <- 0
# x_max <- 1

# Word vs. sens diff


























# updateNumericInput(session = session, inputId = "x_axis_min", value = x_min)
# updateNumericInput(session = session, inputId = "x_axis_max", value = x_max)
# updateSliderInput(session, "override_x", max = x_max, min = x_min, value = c(x_min, x_max))
# 
# updateNumericInput(session = session, inputId = "y_axis_min", value = y_min)
# updateNumericInput(session = session, inputId = "y_axis_max", value = y_max)
# updateSliderInput(session, "override_y", max = y_max, min = y_min, value = c(y_min, y_max))